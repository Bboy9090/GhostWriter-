# Live OCR Captures

Store real device screenshots here. Use the capture helper:

  npm run ocr:capture -- --id OCR-001 --name "Portal sample"

This will create a PNG in this folder and a matching expected text file in
tests/ocr/expected. Populate the expected file with the ground truth text.
